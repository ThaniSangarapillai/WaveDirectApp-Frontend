import React from 'react';

export const HomeScreen = ({ navigation }) => (
  <Screen navigation={navigation} name="Home" />
);
export const OneScreen = ({ navigation }) => (
  <Screen navigation={navigation} name="One" />
);
export const OneScreen = ({ navigation }) => (
  <Screen navigation={navigation} name="Two" />
);
export const OneScreen = ({ navigation }) => (
  <Screen navigation={navigation} name="Three" />
);
